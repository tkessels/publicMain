package org.publicmain.sql;

/**
 * Dieses Package enthält alle Bestandteile des Datenbankmoduls der Anwendung pM nach dem erstellten
 * Schichtenmodell. In diesem Paket sind die Verbindung zur Datenbank, das Erstellen der erforderlichen
 * Tabellen bei erstmaligem Start der Anwendung pM sowie folgende Funktionalitäten bereitgestellt:
 * 
 * - der Benutzer kann mit diesem Modul über die GUI die eigene Chat-Historie betrachten,
 * - der Benutzer kann mit diesem Modul über die GUI die eigene Chat-Historie durchsuchen,
 * - der Benutzer kann mit diesem Modul über die GUI die eigene Chat-Historie löschen,
 * - die Anwendung kann Logs über die aktuelle Sitzung hinaus ablegen,
 * - die Anwendung kann Routinginformationen ablegen.
 *
 * Verantwortlich für das SQL-Package: René Pfaffner
 * 
 * @author ATRM
 * 
 */